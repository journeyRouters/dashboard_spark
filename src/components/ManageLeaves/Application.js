import React from 'react';

const Application = () => {
    return (
        <div>
            <div style={{margin:'1rem'}}><button onClick={()=>alert('This Function is Under Repair')}>Apply For a Leave</button></div>
        </div>
    );
}

export default Application;
